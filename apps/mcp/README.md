# MCP App

This directory is reserved for the standalone MCP server.

It will become the primary machine-write interface for content operations and should reuse shared code from `packages/db` and `packages/services`.
