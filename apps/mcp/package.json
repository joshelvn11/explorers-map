{
  "name": "@explorers-map/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "env EXPLORERS_MAP_WORKSPACE_ROOT=../.. node --env-file-if-exists=../../.env --watch --experimental-strip-types server.ts",
    "start": "env EXPLORERS_MAP_WORKSPACE_ROOT=../.. node --env-file-if-exists=../../.env --experimental-strip-types server.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --experimental-strip-types --test ./*.test.ts"
  },
  "dependencies": {
    "@explorers-map/services": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.25.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
